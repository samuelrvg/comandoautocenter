{
  "name": "comandoautocenter",
  "version": "1.0.0",
  "private": true,
  "description": "Site institucional da Comando Auto Center — Caldas Novas",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./styles.css --minify",
    "watch:css": "tailwindcss -i ./src/input.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
